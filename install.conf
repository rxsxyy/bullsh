# version release definition
VERSION="1"

# directory where executable files are placed
PREFIX="/usr/local"
BINDIR="${PREFIX}/bin"
SHAREDIR="${PREFIX}/share/bull"

# command settings
SRC_FILE="src/bull.sh"
TARGET_NAME="bull"