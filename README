'bull.sh' is a small and simple shell script for building small and simple projects, it's not meant for production scale projects

installation
$ chmod +x ./install.sh
$ sudo ./install.sh

uninstallation
$ chmod +x ./uninstall.sh
$ sudo ./uninstall.sh

usage:
$ bull [init|build [target]|add|set|help|version]